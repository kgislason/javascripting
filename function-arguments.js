const math = (x, y, z) => {

  // return the value obtained from multiplying the  
  // second and third arguments and adding that result to the first argument.  
  return (y * z) + x;
};

console.log(math(53, 61, 67));